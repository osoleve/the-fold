# Nemotron QA Review: 29ef8173 + 187b4fe7

**Date**: 2026-02-04
**Commits**:
- `29ef8173` - feat(contracts): Extend and/c, or/c, not/c for function contracts
- `187b4fe7` - fix(contracts): check ALL domains in and/c, not just first

**Reviewer**: Nemotron (local llama-server)

## Changed Files

- core/types/contracts.ss
- core/types/test-contracts.ss

---

## Review Summary

The implementation is a solid approach to higher-order contracts with combinators. Nemotron identified several areas for consideration:

## 1. Correctness Analysis

### 1.1 `higher-order-contract?`
- ✅ Correctly detects function, dependent, and chaperone contracts
- ✅ Recursively handles nested And/Or/Not
- Note: May need extension if new contract types are added

### 1.2 `wrap-and-contract`
- ✅ Non-procedure path checks flat contracts, errors on function contracts
- ✅ Procedure path wraps for call-time checking
- Note: Error messages are generic; could be more specific

### 1.3 `wrap-or-contract`
- ✅ Correctly separates flat and function contracts
- ✅ Non-procedures only match flat contracts
- ✅ Procedures get wrapped for call-time dispatch

### 1.4 `wrap-or-function`
- ✅ Tries contracts in order (preserves or/c semantics)
- ✅ Arity check before domain check (optimization)
- ✅ First matching contract wins
- Note: Uses `error` for violations rather than returning blame terms

### 1.5 `check-all-domains` (FIXED in 187b4fe7)
- ✅ Now correctly iterates through ALL function contracts
- ✅ Threads wrapped arguments through each check
- Previously: Only checked first contract's domain (bug caught by Gemini)

## 2. Design Decisions

| Decision | Rationale |
|----------|-----------|
| Error on call vs. blame return | Simpler API; violations are typically fatal |
| Arity-first dispatch for or/c | Fast rejection of mismatched contracts |
| Sequential domain checking for and/c | Respects intersection semantics |

## 3. Potential Future Improvements

1. **Richer blame messages** - Include which contract in the combinator failed
2. **Lazy contract evaluation** - Don't check all domains upfront for and/c
3. **Dependent contracts in combinators** - Currently not supported
4. **Contract source locations** - Propagate through combinators

## 4. Test Coverage

- ✅ or/c with multiple function contracts
- ✅ and/c with multiple function contracts
- ✅ and/c with different domains (regression test added)
- ✅ not/c with function contracts
- ✅ Mixed flat and function contracts

## Verdict

**Approved with notes.** The implementation correctly handles the core use cases for higher-order contract combinators. The fix for `check-all-domains` was critical and has been addressed. Future work could improve blame precision and add support for dependent contracts in combinators.

---
*Generated by Nemotron via local llama-server*
