{
  "name": "the-fold-agents",
  "version": "1.0.0",
  "description": "Agent scripts for The Fold",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:watch": "node --test --watch tests/*.test.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
