# Replace __FOLD_ROOT__ with the absolute path to your checkout.
# Example: /home/oso/the-fold/.fold-repl/daemon.log
~/the-fold/.fold-repl/daemon.log ~/the-fold/.fold-repl/test-all.log {
  daily
  rotate 7
  missingok
  notifempty
  compress
  delaycompress
  copytruncate
}
